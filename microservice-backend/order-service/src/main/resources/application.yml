spring:
    application:
        name: order-service
    data:
        mongodb:
            uri: "mongodb+srv://jnexusadmin:jnexusadmin@cluster0.q6nnoki.mongodb.net/js_order_service?retryWrites=true&w=majority"

server:
    port: 9070

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
    instance:
        hostname: localhost