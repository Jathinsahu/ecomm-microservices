spring:
    application:
        name: category-service
    data:
        mongodb:
            uri: "mongodb+srv://jnexusadmin:jnexusadmin@cluster0.q6nnoki.mongodb.net/js_category_service?retryWrites=true&w=majority"

server:
    port: 9000

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
        fetchRegistry: true
        registerWithEureka: true
    instance:
        hostname: localhost